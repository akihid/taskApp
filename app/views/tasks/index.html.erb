<h1>Tasks#index</h1>

<table>
  <th>タスク名</th>
  <th>内容</th>

    <% @tasks.each do |task| %>
    <tr>
      <div>
        <td><%= task.title %></td>
        <td><%= task.content %></td>
        <td><%= link_to "詳細", task_path(task.id)%></td>
        <td><%= link_to "編集" , edit_task_path(task.id) %></td>
        <td><%= link_to "削除", task_path(task.id), method: :delete, data:{ confirm: "削除してもよろしいですか？" } %></td>
      </div>
      </tr>
    <% end %>

</table>

<%= link_to "新規作成" , new_task_path%>
